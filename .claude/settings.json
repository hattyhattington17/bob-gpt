{
  "enabledPlugins": {
    "pyright-lsp@claude-plugins-official": true
  },
  "hooks": {
    "Stop": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; echo \"$f\" | grep -q '\\.py$' && uv run ruff format \"$f\" && uv run ruff check --fix \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Running ruff..."
          }
        ]
      }
    ]
  }
}
